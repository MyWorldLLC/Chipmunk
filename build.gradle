apply plugin: 'java'

sourceCompatibility = 1.5
version = '1.0'

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.+'
}

uploadArchives {
    repositories {
       flatDir {
           dirs 'release'
       }
    }
}
